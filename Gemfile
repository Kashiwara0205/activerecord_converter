source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in activerecord_converter.gemspec.
gemspec

group :development, :test do
  gem 'mysql2'
  gem 'pokotarou'
end

# To use a debugger
# gem 'byebug', group: [:development, :test]
